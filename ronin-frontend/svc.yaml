apiVersion: v1
kind: Service
metadata:
  name: ronin-frontend-service
  namespace: ronin-namespace
spec:
  selector:
    app: ronin-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: NodePort
