apiVersion: v1
kind: Service
metadata:
  name: ronin-backend
  namespace: ronin-namespace
spec:
  selector:
    app: ronin-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
